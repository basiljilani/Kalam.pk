---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["bg-white p-6 rounded-lg shadow-lg hover:shadow-xl transition duration-300", className]}>
  <slot />
</div>